# README

```
cd catkin_ws/src/ros_rpi_usb_power/src     # cd to the directory with your node
chown root:root usb_switch_listener # change ownship to root
chmod a+rx usb_switch_listener      # set as executable by all
chmod u+s usb_switch_listener       # set the setuid bit
```


